[project]
name = "temporal"
version = "0.1.0"
description = "Temporal knowledge graph for cognitive agents"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=7.0", "pytest-asyncio>=0.21"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
